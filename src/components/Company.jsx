import React from 'react';
import './Company.scss';

const Comapny = ({ company, link }) => {

    return (
        <div className="company">
            Company
        </div>
    );
};

export default Comapny;
